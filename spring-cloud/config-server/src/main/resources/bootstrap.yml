spring:
        application:
                name: thisme
        profiles:
                active: mydev
        cloud:
                config:
                        server:
                                git:
                                        uri: https://github.com/besttheway/myfirst.git
                                        search-paths: one
                                        username:
                                        password:
                                        default-label: newVersion
        rabbitmq:
                addresses: amqp://localhost:5672
                username: guest
                password: guest