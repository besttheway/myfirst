spring:
        application:
                name: thisme
        cloud:
                config:
                        server:
                                git:
                                        uri: https://github.com/besttheway/myfirst.git
                                        search-paths: one
                                        username:
                                        password:
                        label: newVersion
                        profile: mydev